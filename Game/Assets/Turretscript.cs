using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Turretscript : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    // this script will handle, shooter id, the enemy hitscan, the shooting of projectiles and  the turret's destruction after use.
    
    public void Rotation()
    {
        // move from left to right and scan for enemies

        // if enemy is seen, stop rotation and , shoot in that direction 

        // if enemy moves out of view, stop shooting and keep scanning left to right


        // destroy or fade out turret after a few seconds
    }

}
